[alembic]
script_location = app/db/migrations
prepend_sys_path = .
# Overridden at runtime by DATABASE_URL env var in env.py; this value is the
# Docker Compose default and is also used for `alembic` offline SQL generation.
sqlalchemy.url = postgresql+asyncpg://postgres:postgres@db:5432/code_reviewer

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
